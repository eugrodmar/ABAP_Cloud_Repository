projection implementation in class ZBP_C_PROF_17 unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_PROF_17 alias ZcProf17
extensible
use etag
{
  use create;
  use update;
  use delete;

use action subida_sueldo;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}